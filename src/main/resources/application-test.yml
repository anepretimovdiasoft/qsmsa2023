server:
  port: 8081

spring:
  h2:
    console:
      enabled: true

  #h2
  datasource:
    url: jdbc:h2:mem:testdb

  liquibase:
    enabled: false

  jpa:
    #generate-ddl: false
    generate-ddl: true
    hibernate:
      #ddl-auto: none
      ddl-auto: create-drop
    show-sql: true