server:
  port: 8081

spring:
  h2:
    console:
      enabled: true

  jpa:
    #generate-ddl: false
    generate-ddl: true
    hibernate:
      #ddl-auto: none
      ddl-auto: create-drop
    show-sql: true

  datasource:
    url: jdbc:h2:mem:testdb